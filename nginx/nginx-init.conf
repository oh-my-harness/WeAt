# 第一阶段：仅 HTTP，用于 certbot 申请证书
# 不代理后端（临时容器没有 backend 上游）
server {
    listen 80;
    server_name oh-my-harness.site;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}
